<h1>Add a New List</h1>

<%= form_for :list, url: lists_path do |f| %>
    <div>
        <%= f.label :name %>
        <br>
        <%= f.text_field :name %>
    </div>

    <div>
        <%= f.submit %>
    </div>
<% end %>